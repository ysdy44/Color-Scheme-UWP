<UserControl
    x:Class="配色pro.Controls.LabelControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:配色pro.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="50"
    d:DesignWidth="50">

    <Button x:Name="Button" Width="Auto" Height="Auto" HorizontalAlignment="Stretch" Padding="0" VerticalAlignment="Stretch" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" Style="{StaticResource NavigationBackButtonNormalStyle}">
        <Grid>
            <PathIcon x:Name="SeletedPath" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{ThemeResource SystemBaseHighColor}" RenderTransformOrigin="0.5,0.5"/>
            <PathIcon x:Name="UsualPath" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{ThemeResource SystemBaseHighColor}" />
            <Rectangle x:Name="Line" Visibility="Collapsed" Height="2"  VerticalAlignment="Bottom" Fill="{ThemeResource SystemBaseHighColor}" />
        </Grid>
    </Button>
</UserControl>
