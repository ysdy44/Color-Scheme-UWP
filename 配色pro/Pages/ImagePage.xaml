<Page
    x:Class="配色pro.Pages.ImagePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    xmlns:local="using:配色pro.Controls"
    xmlns:lib="using:配色pro.Library"
    mc:Ignorable="d"
    >    
    
    <Page.Resources>
        <lib:CanvasOperator x:Name="Operator" DestinationControl="{Binding ElementName=CanvasControl}" />
    </Page.Resources>

    <Grid>

        <canvas:CanvasControl x:Name="CanvasControl"/>

        <Canvas x:Name="Canvas" IsHitTestVisible="False" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <local:StrawControl x:Name="StrawControl"/>
        </Canvas>

        <Button x:Name="Button" Width="Auto" Height="140"   HorizontalAlignment="Center"   Style="{StaticResource NavigationBackButtonNormalStyle}">
            <Grid MinWidth="130" Height="130" Margin="4">
                <FontIcon Glyph="&#xEB9F;" FontSize="44" FontFamily="Segoe MDL2 Assets"  />
                <TextBlock Text="Select a picture" FontSize="16"  FontFamily="微软雅黑"  HorizontalAlignment="Center" VerticalAlignment="Bottom" />
            </Grid>
        </Button>

    </Grid>
</Page>
